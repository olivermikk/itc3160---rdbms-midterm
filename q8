SELECT a.FIRSTNAME, a.LASTNAME, b.RATING
FROM WORKER a
INNER JOIN ASSIGN b on a.EMPID = b.EMPID
WHERE EXISTS
(SELECT * FROM PROJECT c
WHERE b.PROJNO= c.PROJNO AND c.PROJMGRID = (
SELECT EMPIDFROM worker
WHERE FIRSTNAME= 'Michael' AND LASTNAME= 'Burns'))
ORDER BY a.LASTNAME ASC;
